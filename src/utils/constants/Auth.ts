export enum AuthResponse {
    USER_ALREADY_EXITS = 'PreSignUp+failed+with+error+A+user+with+the+same+email+address+exists.+'
}

export enum AuthMessage {
    USER_ALREADY_EXITS = 'A user with the same email address exists'
}

export enum AuthEvent {
    CUSTOM_STATE_FAILURE = 'customState_failure'
}